import { MissingPersonCase, Alert } from "../../types";

// Emptying mock datasets to ensure only live Firebase data is displayed.
export const mockCases: MissingPersonCase[] = [];

export const mockAlerts: Alert[] = [];
